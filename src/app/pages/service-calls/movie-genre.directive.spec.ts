import { MovieGenreDirective } from './movie-genre.directive';

describe('MovieGenreDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieGenreDirective();
    expect(directive).toBeTruthy();
  });
});
