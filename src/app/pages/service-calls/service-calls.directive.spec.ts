import { ServiceCallsDirective } from './service-calls.directive';

describe('ServiceCallsDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceCallsDirective();
    expect(directive).toBeTruthy();
  });
});
